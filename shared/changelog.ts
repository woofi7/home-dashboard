// AUTO-GENERATED by nuxt.config.ts — do not edit
export type ChangelogEntry = { version: string; notes: string[] }

export const CHANGELOG: ChangelogEntry[] = [
  { version: "v1.3.2", notes: ["Add Mealie service widget showing recipe count, last added recipe, categories, and tags. Uses the public explore API - no authentication required."] },
  { version: "v1.3.1", notes: ["Show login modal when completing a task requires authentication (instead of silently reverting)","Add emerald dot inside the Edit button when session is authenticated"] },
  { version: "v1.3.0", notes: ["Require auth on all admin endpoints; strip admin token from /api/config","Add SSRF allowlist to widget proxy endpoints","Filter dangerous URL schemes from links and server-side fetches","Replace static session cookie with HMAC-signed timestamped token","Block ADMIN_TOKEN and SESSION_KEY from config variable substitution","Add assertAuth and CSRF state to Google OAuth flow","Detect uploaded background type from magic bytes","Rate-limit failed admin login attempts per IP","Drop unsafe-eval from CSP","Strip credential keys from widgets in /api/config","Apply scheme guard to Google Calendar and background photo links","Add assertAuth to task completion endpoint","Return hasClientSecret flag instead of client secret value","Public transit widget with configurable URL and pass count display","Auto-refresh for weather, calendar, and background at independent intervals","Admin widget hub at /admin/widgets with per-widget enable/disable toggles","Widget settings pages nested under /admin/widgets/*","Full breadcrumb trail: Dashboard / Admin / Widgets / Page","Enable/disable toggle on each individual widget settings page"] },
  { version: "v1.2.2", notes: ["## What's new","### Dockhand widget","Aggregates container stats across all Dockhand environments in a single widget. Shows running, stopped, unhealthy, and pending-update counts. Uses username/password auth via the Dockhand session API."] },
  { version: "v1.2.1", notes: ["**Docker-first auto healthcheck**: services with no explicit healthcheck use their Docker container state, falling back to an HTTP ping of the service URL when the container is missing or unhealthy.","**Status method in the tooltip**: hovering the status dot names what produced it (`Docker container[ on <server>]: <status>` or `HTTP ping <url>: Reachable/Unreachable`).","**Server/container on the card**: shown under the service name as `server/container`.","**Inline widget errors**: widget failures show the full reason on the card instead of only on hover, and long messages wrap instead of overflowing.","**Docker API port default**: hosts configured as `host: http://x` with no explicit port were queried on port 80 instead of the Docker API; they now default to 2375, so remote servers resolve their containers.","**Local Docker always monitored**: the built-in `local` server is now always queried via the host Docker socket (or `DOCKER_HOST`), even alongside remote servers. Define an explicit `local` entry to override its connection.","**Scheme-less URLs**: links and API calls for URLs entered without a scheme (e.g. `10.0.1.2:8200`) no longer resolve relative to the current page. Browser links default to `http://`; server-side widget and healthcheck fetches try `https` first and fall back to `http`, so public TLS hosts and plain-http LAN hosts both work. Fixes widgets (e.g. Uptime Kuma) whose auth header was dropped on the http to https redirect."] },
  { version: "v1.2.0", notes: ["**Google Tasks on the calendar panel**: Tasks now appear as a checkbox to-do list above events, with their own card styling and a due-date pill (yellow when overdue, green otherwise).","**Complete tasks from the dashboard**: clicking a task row opens a confirmation, then marks it complete in Google Tasks (optimistic, reverts on failure). A dedicated open-in-browser icon links to the task.","**Display modes** (admin-configurable): *Everything* (all overdue plus today and upcoming within the days-ahead window) or *Overdue* (overdue plus today only).","Requires the read/write Tasks scope: reconnect your Google account once after upgrading to enable completion.","**Appearance settings**: background display settings (position, overlay, blur, fade-in) moved into the Background section; the Appearance section now holds only the UI element styles.","Admin calendar panel surfaces Google Tasks API errors and refreshes its preview immediately on save.","Register the missing `key` icon in the admin calendar setup."] },
  { version: "v1.1.4", notes: ["**qBittorrent widget**: Support qBittorrent 5.2.x, which renamed the WebUI session cookie from `SID` to `QBT_SID_<port>`. The widget now authenticates correctly on 5.2.x while keeping 5.1.x working."] },
  { version: "v1.1.3", notes: ["## Fixes","**Offline banner now shows the real snapshot age.** `/api/*` is cached NetworkFirst by the service worker, so an offline fetch still resolved from cache and the stores stamped the page-load time, making the banner always read \"just now\". Snapshot time is now read from the response `Date` header (preserved in the cache), and the banner also shows the absolute snapshot time.","**Deleted services and bookmarks no longer come back.** Removing an item in edit mode was undone on save: the `reorderGroups` orphan guard (which protects against drag-and-drop desync) treated an intentional deletion the same as a dropped item and rewrote it to disk. The client now reports which items it deliberately removed so the guard skips them while still rescuing genuinely dropped ones. The group sections also kept a stale local copy that left a deleted item visible until reload; they now re-sync on edit exit."] },
  { version: "v1.1.2", notes: ["**Offline banner now appears** (and within ~2s): the `/api/healthcheck` reachability ping was hanging on the unreachable host with no timeout, so `online` never flipped to `false`. It now uses a 2s timeout.","**Shorter dark-screen on launch offline:** workbox `networkTimeoutSeconds` lowered from 5s to 2s, so the cached snapshot is served faster."] },
  { version: "v1.1.1", notes: ["The previously-registered service worker stays active until it updates. Hard-reload (or unregister the SW in DevTools) after deploying so the new worker takes over.","`/api/` `fetch()` caching (NetworkFirst) is unchanged; only the SPA navigation fallback is affected."] },
  { version: "v1.1.0", notes: ["Installable PWA (`@vite-pwa/nuxt`) with manifest, full icon set (192/512, maskable, apple-touch-icon, favicon) and iOS meta.","Built as an SPA with a precached app shell and `NetworkFirst` caching for `/api/*`, so the dashboard opens offline showing the last snapshot.","Offline indicator banner; service health dots turn yellow when unreachable; edit mode and admin are disabled while offline."] },
  { version: "v1.0.9", notes: ["CalendarPanel: 250px max height with scrollable left-side scrollbar styled to match app theme"] },
  { version: "v1.0.8", notes: ["Bookmark auto-sort by click count with toggle in admin settings","Collapse toggle for service and bookmark groups","Bookmark group header border matching service groups","Calendar panel: removed background container, event cards only","Backup system: .bak files moved to backup/ subdirectory","Auto-backup restore: list, preview, and per-file restore in admin panel","Reusable YamlFilePreview component with lazy-load and slot API","Zip restore: client-side file preview with per-file selection before importing","Custom color palette: replace amber preset with fully user-configurable colors","Drag handle hover highlight across all draggable elements"] },
  { version: "v1.0.7", notes: ["Fix Tugtainer widget not respecting admin field config (toggling fields had no effect on the dashboard)","Sort admin index nav tiles alphabetically","Sort widget fields catalog alphabetically by name","Highlight entire active field row on hover in the widget fields admin"] },
  { version: "v1.0.6", notes: ["Tugtainer widget: per-host updates available, total containers, unused and dangling images","Backrest widget: switched to basic auth (username + password) to support Backrest password protection","Tugtainer host prefix format changed to 'Host - Field' style"] },
  { version: "v1.0.5", notes: ["Backrest widget with per-repo stats (last backup, snapshots, disk size). Per-service healthcheck on status dot. Fixes version display in Docker containers: APP_VERSION build arg baked at build time, CI pipeline updated to pass the tag."] },
  { version: "v1.0.4", notes: ["Link target setting: open services and bookmarks in new tab or same tab","Backup and Restore admin page: download/upload a zip of all config files","Config backup button in admin header: timestamped zip download","Section rename with duplicate name prevention and backend data-loss guards","Build version tag displayed in bottom-left corner of dashboard","Docker server admin page","Rewrite Uptime Kuma widget to use Prometheus `/metrics` with Basic auth","Fix orphan guard incorrectly recovering items from intentionally deleted groups","Fix widget config wipe when moving services between groups","Fix edit modal UX: icon browse button, scroll leaking, widget dropdown overlay","Fix widget test credentials (env var substitution in service-creds endpoint)","Improve add button visibility over background images in edit mode"] },
  { version: "v1.0.3", notes: ["Zero-config boot with always-visible Edit button and first-time admin setup wizard","Appearance admin page: section/card style options with layered preview","Admin settings and weather pages with save bar in header and weather cache","Drag-and-drop field reordering in widget admin with cache invalidation","Bookmark counter enable/disable toggle and reset in admin settings","Calendar display settings admin; collapse setup steps once connected","New widgets: ASF, Beszel, Uptime Kuma, Restic","Portainer widget: Healthy/Unhealthy container count fields","Widget tests for ASF, Beszel, Uptime Kuma, and Restic"] },
  { version: "v1.0.2", notes: ["Google Calendar admin page with OAuth setup guide and live event preview","Background image admin page: multi-provider support (URL, Unsplash, local), blur control","Background settings panel with routing","Full project README","Background blur applied correctly after provider switch"] },
  { version: "v1.0.1", notes: ["Remove skeleton animations for snappier feel","Use `shallowRef` for refresh data to reduce reactivity overhead","Fade transition on background image load","Fix white overscroll strip on mobile by setting `html`/`body` background to base color"] },
]

export function getEntry(version: string): ChangelogEntry | undefined {
  return CHANGELOG.find(e => e.version === version)
}
